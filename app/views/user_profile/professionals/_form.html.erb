<%= form_with(model: user_profile_professional, class: "contents") do |form| %>
  <% if user_profile_professional.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-lg mt-3">
      <h2><%= pluralize(user_profile_professional.errors.count, "error") %> prohibited this user_profile_professional from being saved:</h2>

      <ul>
        <% user_profile_professional.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="inline">
    <%= form.submit class: "rounded-lg py-3 px-5 bg-blue-600 text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>
